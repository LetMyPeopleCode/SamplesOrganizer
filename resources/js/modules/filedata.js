// includes functionality for populating the file data and tags areas
const filedata = {};
filedata.all_tags = [];


filedata.read_tags = () =>{
  // read the tags from the tags collection in the database into memory for quick access


}

filedata.parse_file = () => {
  //go through the stats and path and try to extract BPM, file name, producer, license file
  //we should have the filepicker
   
}